name: Mobile Build

on:
  push:
    tags: ['v*']
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Add Android platform
        run: npx cap add android

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: vibecart-android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14

  # Release build (uncomment when signing is configured)
  # build-android-release:
  #   name: Build Android Release
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: 'npm'
  #     - run: npm ci
  #     - uses: actions/setup-java@v4
  #       with:
  #         distribution: 'temurin'
  #         java-version: '17'
  #     - run: npx cap add android && npx cap sync android
  #     - name: Decode keystore
  #       run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks
  #     - name: Build release AAB
  #       working-directory: android
  #       env:
  #         KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
  #         KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
  #         KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
  #       run: ./gradlew bundleRelease
  #     - uses: actions/upload-artifact@v7
  #       with:
  #         name: vibecart-android-release
  #         path: android/app/build/outputs/bundle/release/app-release.aab

  # iOS builds require macOS runner (expensive)
  # Use Codemagic or local Xcode builds instead
